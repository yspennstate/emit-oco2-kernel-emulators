\section{The physical domain, the training targets and two tests fixed in advance}\label{sec:domain}

The all-band scores of Table~\ref{tab:tails} mix three things: the error of the emulator, entries
where the retrieval is not defined, and entries where it is defined but badly conditioned. This
section separates them. It describes the physical domain of the retrieval and the entries of the
table that fall outside it, scores the main pipeline on that domain with its coverage and failures,
asks whether the out-of-domain entries matter when they are used as training targets, and repeats
the forward--inverse comparison on a fresh partition and on a second radiative-transfer code, both
fixed in advance.

\subsection{The physical domain}
Write $t=Y_2+Y_3$, $s=Y_4$ and $q=1-\rho s$, with $\rho=0.7$. The retrieval analysis of
Section~\ref{sec:theory} holds on
\[
  \mathcal A=\{t>0,\ 0\le s<1\},
\]
which is a sufficient domain for the physical retrieval rather than the largest set on which the
rational formulas are defined: a negative albedo with $t>0$ gives $q>0$ and a well-defined inverse,
and $q<0$ violates the physical hypothesis without being a zero of a denominator. At $q=0$ the
forward formula is undefined, and at $t=0$, wherever it is defined, $L=Y_1$ does not depend on the
reflectance, so the band carries no information about it. A small positive transmission makes the
inverse ill-conditioned but not undefined.

\begin{table}[t]
\centering
\input{table_domain_audit}
\caption{The table against the physical domain, over all $23{,}313\times285=6{,}644{,}205$
state--band entries. The categories overlap. Small positive transmission is a conditioning
property, not a violation of $t>0$; an albedo outside $[0,1)$ violates the physical domain
without necessarily leaving the algebraic domain of the inverse.}
\label{tab:domain}
\end{table}

Table~\ref{tab:domain} counts the entries of the table outside $\mathcal A$. There are 438
nonpositive transmissions, the smallest $-4.5\times10^{-20}$, and $32{,}130$ transmissions below
$10^{-12}$; $9{,}858$ negative albedos, the smallest $-0.0724$; and 454 albedos at or above one,
214 of which equal $2$ exactly, which can only be a fill value. At 448 entries $q\le0$, the
smallest $-0.4$. In all, $10{,}356$ entries ($0.156\%$) lie outside $\mathcal A$. They occupy 227
bands but are concentrated near 1841--1908 and 2485--2493\,nm, and $18{,}859$ of the $23{,}313$
states are admissible at every band.

A stricter domain adds a conditioning floor,
\[
 \mathcal C=\mathcal A\cap\{t\ge\tau,\ q\ge\kappa\},
 \qquad \tau=10^{-12},\quad\kappa=0.3,
\]
with $\tau$ in the flux units of the table; at $\rho=0.7$ the albedo condition already gives
$q>0.3$. Inverting the exact tabulated components returns the reflectance to within
floating-point error, and that error is informative:

\begin{center}
\begin{tabular}{lrr}
\toprule
domain & round-trip RMSE (reflectance) & coverage \\
\midrule
all entries & $7.38\times10^{-3}$ & $100\%$ \\
$\mathcal A$ & $4.27\times10^{-3}$ & $99.844\%$ \\
$\mathcal C$ & $1.08\times10^{-9}$ & $99.516\%$ \\
\bottomrule
\end{tabular}
\end{center}

In exact arithmetic the round trip is exact wherever it is defined. In floating point, adding
$\rho t/q$ to $Y_1$ and subtracting $Y_1$ again loses the added term when it is small relative to
the spacing of numbers near $Y_1$, which is what happens at the smallest transmissions. Removing
$0.33\%$ of the entries takes the round-trip error from $4\times10^{-3}$ to $10^{-9}$; what remains
of the all-band reflectance tail on $\mathcal C$ belongs to the emulators.

\subsection{The main pipeline on the physical domain}\label{sec:tq-domain}
The families of the main comparison were refitted on the ten splits at both network widths with
their float64 test predictions kept; these are the raw arm of the experiment in
Section~\ref{sec:tq-policy}. Each family is scored on the test entries that satisfy
\[
 t\ge\tau_0T_{\rm train},\qquad 0\le s<1,\qquad q\ge0.3,
\]
where $T_{\rm train}$ is the median positive transmission of the split's training block ($4.67$ at
split 101) and $\tau_0\in\{10^{-12},10^{-3},10^{-2}\}$; the three floors were fixed before the
first fit, and at $\rho=0.7$ the albedo condition already gives $q>0.3$. An inversion fails when
its predicted denominator is not positive or its value is not finite. Failures are counted, and
the percentiles are taken over the successful inversions. Coverage depends on the truth alone and
is the same for every family. The mask describes where the retrieval is well posed; it is an
evaluation domain, not a filter that a retrieval could apply without knowing the true components.

\begin{table}[t]
\centering
\resizebox{\textwidth}{!}{\input{table_tq_domain}}
\caption{The main pipeline on the physical domain: the raw arm, mean $\pm$ sample standard deviation
over [[TQ-SEEDS]] splits. Radiance is the mean relative error in percent, and the all-band column is
the unrestricted inverse of Table~\ref{tab:tails}. At each floor $\tau_0$, the failed column is the
percentage of covered entries at which the inversion fails, and $p_{95}$ is the 95th percentile of the
absolute reflectance error over the successful inversions, in percentage points.}
\label{tab:tq-domain}
\end{table}

[[TQ-DOMAIN]]

\subsection{Training on physically admissible targets}\label{sec:tq-policy}
The training pipeline standardizes, compresses and fits the targets as they come, including the
$0.156\%$ of entries outside $\mathcal A$ and the fill values among them. Scoring on $\mathcal A$
cannot tell whether those entries shaped the fitted representations, the principal components or
the albedo plateau of Section~\ref{sec:scaling}; only refitting can. We therefore ran the pipeline
under three training policies: the original one (\emph{raw}); removal of every training state with
an entry outside $\mathcal A$ at any band (\emph{admissible}), with no clipping or imputation; and
an unfiltered random subset of the raw training rows of the same size as the admissible set
(\emph{matched}), which separates the effect of the filter from that of losing rows. Whole states
are removed so that every remaining spectrum is complete for the principal-component step; this
changes the training distribution, and the matched arm measures how much of any difference is
only the loss of $19\%$ of the rows. The validation rows, the validation loss and the test blocks
are those of the raw pipeline in all three arms, and every arm is scored on the same test entries
with a flux scale taken from the original training block.

The arms, the two network configurations of Table~\ref{tab:results} and Table~\ref{tab:wide}, the
seeds 101--110, the scoring and the summaries reported below were fixed in a written protocol
before the first run, and every run was completed and is reported. Each run records the digests of
its data and of its split indices, and saves the float64 predictions of every family. Splits
101--105 ran on one machine and splits 106--110 on another, and the three arms of a split always ran
on the same machine, so every paired difference below is taken within one computing environment
(Table~\ref{tab:tq-runs}).

\begin{table}[t]
\centering
\resizebox{\textwidth}{!}{\input{table_tq_policy}}
\caption{Training on admissible targets: paired differences from the raw arm, arm minus raw, mean
$\pm$ sample standard deviation over [[TQ-SEEDS]] splits. Radiance differences are in percentage
points of relative error, albedo differences in percentage points of the relative albedo error, and
the reflectance percentiles in percentage points; the physical $p_{95}$ uses the floor
$\tau_0=10^{-12}$. The columns headed lower count the splits on which the arm is below the raw
arm.}
\label{tab:tq-policy}
\end{table}

\begin{table}[t]
\centering
\resizebox{\textwidth}{!}{\input{table_tq_reversal}}
\caption{The convex stack against the kernel on features in each arm: the radiance difference, stack
minus feature kernel, in percentage points, and the ratio of their 95th-percentile reflectance errors
over all bands and on the physical domain at the three floors; mean $\pm$ sample standard deviation
over [[TQ-SEEDS]] splits. The columns headed $>1$ count the splits on which the stack has the
larger tail, over all bands and at $\tau_0=10^{-12}$.}
\label{tab:tq-reversal}
\end{table}

[[TQ-POLICY]]

\subsection{Where the reflectance error sits}

\begin{figure}[p]
\centering
\includegraphics[height=0.80\textheight,width=\textwidth,keepaspectratio]{emit_quantiles_top_models.png}
\caption{Residual quantile bands across the test set of split 101, band by band, for six families of
the main pipeline. The envelopes are nested percentiles of the residual at each wavelength:
$25$--$75$ in red, $5$--$25$ and $75$--$95$ in blue, $2.5$--$5$ and $95$--$97.5$ in green,
$0.5$--$2.5$ and $97.5$--$99.5$ in grey, with the median as a thin black line. From the left: the
direct-flux residual $Y_2-\widehat{Y_2}$ in physical units; the radiance residual at $\rho=0.7$;
the reflectance residual over all bands; and the reflectance residual on the physical domain with
the floor $\tau_0=10^{-12}$. The last two columns share a vertical scale of $\pm2$ points.}
\label{fig:bands}
\end{figure}

Figure~\ref{fig:bands} shows where along the spectrum the errors of one split sit. The component
and radiance residuals are smooth in wavelength and widen where the transmitted flux is small, so
the families differ mainly in the amplitude of a shared shape. The reflectance residual does not
inherit that shape. Over all bands its outer envelopes open into vertical walls, bands where the
95th percentile of the absolute error exceeds two points, and the walls have two parts. A common
core of 54 bands, at 1126--1134, 1342--1469, 1796--1952 and 2412--2493\,nm, is a wall for every
family, the most accurate included: there the denominator, not the emulator, sets the width.
Outside the core each family adds bands in proportion to its inaccuracy: none for the kernel on
features, 3 for the stack, 12 for the input-scaled kernel, 22 for the corrected network, 29 for
the network and 79 for the cubic ridge, of a 133-band union. Restricting to the physical domain
changes none of these counts, and neither does the stricter floor $\tau_0=10^{-3}$, which removes
$6.3\%$ of the entries: the walls come from transmissions that are small but far above either
floor, where the retrieval divides a small component error by a small denominator. The feature
kernel's advantage in the tail is that it adds no wall of its own.

Theorem~\ref{thm:transfer} locates the walls in advance. Its item (i) holds at every entry, so at
each band the 95th percentile of the constrained retrieval error is at most the 95th percentile of
$\min\{R,e_R/t\}$, which uses only the component errors and the true transmission. On split 101,
with $R=0.9$ and $S$ the largest training albedo not above one, the constrained retrieval on the
physical domain has exactly the walls of Figure~\ref{fig:bands}, family by family, and no entry
violates the pointwise inequality. For every family each wall lies in the set of bands where the
bound's 95th percentile exceeds two points; that set is larger by 8 to 21 bands, and on the walls
the bound exceeds the realized 95th percentile by a median factor of $1.30$ to $1.38$, depending on
the family, and never by more than $4.5$. The bands marked by the bound for all six families
number 64 and include the whole common core of 54.

\subsection{A fresh partition, fixed in advance}\label{sec:confirm}
The comparisons above use partitions that were in view while the families were developed. To test
the reversal against that selection, a simplified pipeline was frozen together with one hypothesis
and evaluated once on a new partition. Its six families are the cubic ridge, a $3\times512$ network
(GELU activations, Adam) trained on the 285 standardized bands, the residual kernel of that
network, the input-scaled Mat\'ern kernel, the Mat\'ern kernel on the network's last hidden layer,
and a convex stack of the cubic, the corrected network, the input-scaled kernel and the feature
kernel. Every hyperparameter was copied from development on seed 101 and none was re-tuned. The
partition holds out 3{,}497 states, $15\%$ of the table, drawn with a seed used in no earlier run;
the remaining 19{,}816 states train the families, 1{,}982 of them serving only as the network's
early-stopping set. The held-out states belong to the same table as every other split, so this
evaluation guards against the choice of split and of hypothesis rather than testing
generalization to new states.

The hypothesis was that the family with the lowest radiance error does not have the lowest
all-band 95th-percentile reflectance error, and that its all-band 95th percentile is strictly larger
than that of the residual-corrected network.

\IfFileExists{table_confirmation.tex}{%
\begin{table}[t]
\centering
\resizebox{\textwidth}{!}{\input{table_confirmation}}
\caption{The fresh partition: 3{,}497 held-out states, evaluated once. The screened columns use
$t\ge10^{-12}$ and $q\ge0.3$, which covers $99.5327\%$ of the $996{,}645$ entries; no inversion is
non-finite in any family. The network here is the $3\times512$ GELU network of the frozen pipeline,
trained with Adam, not the network of Table~\ref{tab:results}.}
\label{tab:confirm}
\end{table}}{}

The reversal reproduces (Table~\ref{tab:confirm}). The convex stack has the lowest radiance error of
the six, $0.0687\%$, and the fourth-lowest reflectance tail; the kernel on learned features has a
larger radiance error, $0.0828\%$, and an all-band 95th percentile smaller by a factor of $6.60$, and
by $5.54$ on the screened entries. Both factors are close to the $6.13$ and $4.99$ of the same
pipeline on its development split, seed 101. The second clause of the hypothesis fails: the stack's
all-band 95th percentile is $25.82$ points and the corrected network's $26.07$, smaller by $0.25$
points where the hypothesis required it to be larger. The qualitative statement survives a single
read of a fresh partition, while the order among the four intermediate families does not separate at
this sample size.

\subsection{A second radiative-transfer code}\label{sec:lrt}
Every comparison so far concerns one simulator and one table. The corpus of \cite{pkan} pairs 6S
\cite{vermote} with libRadtran \cite{mayer,emde} over the thirteen Sentinel-2 bands and gives, for each
atmospheric state and band, the path reflectance $\rho_{\rm path}$, the total transmittance $T$ and the
spherical albedo $S$ computed by libRadtran. The top-of-atmosphere reflectance is
$\rho_{\rm path}+\rho T/(1-\rho S)$, which is \eqref{eq:fwd} with $Y_1=\rho_{\rm path}$, $t=T$ and
$Y_4=S$, so the retrieval \eqref{eq:inv}, the physical domain and Theorem~\ref{thm:transfer} apply
without change. We use the 9{,}722 of its 50{,}000 states that carry all thirteen bands; the others lack
band B10 at 1372\,nm, which lies in a strong water-vapour absorption band and is nearly opaque. Its
median transmittance is $9\times10^{-6}$ and a third of its entries lie below $10^{-6}$, while the
first percentile of every other band is above $0.2$. The seven state variables are the inputs, and the
driver's four components are filled with $Y_2=T$ and a negligible $Y_3=10^{-6}T$.

The pipeline of Section~\ref{sec:methods} was run on these arrays with no change to any family or
hyperparameter grid, at output rank 13 (all bands), on ten splits of the same form (7{,}875 training,
875 validation and 972 test states), and scored as in Section~\ref{sec:tq-domain}. None of these states
played a part in developing the families. Three hypotheses and their decision rule were fixed in a
written protocol before the first fit: (H1) the family with the lowest radiance error does not have the
lowest all-band 95th-percentile reflectance error; (H2) the convex stack is no worse than the kernel on
features in radiance and has the larger all-band tail; (H3) restricting the evaluation to
transmissions above $10^{-3}T_{\rm train}$ brings the families' reflectance tails into closer rank
agreement with their radiance errors, measured by Kendall's $\tau$. Each was to be accepted if it held
on at least nine of the ten splits.

\begin{table}[t]
\centering
\resizebox{\textwidth}{!}{\input{table_lrt}}
\caption{The pipeline on libRadtran, mean $\pm$ sample standard deviation over [[LRT-SEEDS]] splits,
with the columns of Table~\ref{tab:tq-domain}.}
\label{tab:lrt}
\end{table}

[[LRT-RESULTS]]
